export interface Quiz {
    createdAt: string;
    durationMinutes: number
    fileId: string
    id: string
    status: string
    title: string
    totalQuestions: number
    updatedAt: string
    userId: string
}